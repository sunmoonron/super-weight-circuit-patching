# super-weight-circuit-patching
Introduces novel "Mechanistic Surgery" - treating the superweight as a "black box" computational node, excising it, and forcing the network to reveal the underlying circuit by regrowing it in a constrained (aka sparse) environment
